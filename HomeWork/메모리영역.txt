 1. 코드영역
 // exe 파일 정도의 크기를 가진다
	상수
	함수의 설계도

 2. 데이터 영역
 전역변수 데이터들이 존재하게 된다
 3. 힙영역
 // 동적 메모리가 들어간다

 4. 스택영역
 // 운영체제가 알아서 
 // 보통 1메가면 충분하다 <= 이 크기도 조절가능한데 안함
	함수의 설계도 대로 실행되는 실행 흐름의 메모리가 존재하는 곳
	새로운 실행 흐름이 실행되면 메모리가 할당 되었다가
	종료되면 메모리가 소멸하는 곳
