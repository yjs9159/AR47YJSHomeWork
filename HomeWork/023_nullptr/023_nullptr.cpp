#include <iostream>

int main()
{
    int Value = 10;

    // nullptr == 0이라고 부른다
    // 주소를 가리키지 않는다 아직 아무도 가리키고 있지 않다
    // 보통은 nullptr로 초기화한다
    int* Ptr = nullptr;

    // *Ptr = 200;
    // 0번지를 사용하면 안된다
}