#include <iostream>
#include <vector>

// 이제부터 자료형 unsigned __int64를 size_t라고 부르겠다
// typedef        unsigned __int64 size_t;
// insigned __int64 == size_t

// 유저가 정의한 자료형이라고 부른다
// 사용자 정의 자료형의 일종

// typedef int myint; => int를 myint로 사용하겠다

// typedef myint mymyint; => myint를 mymyint로 사용하겠다

// using int myint; => typedef와 using은 똑같은 거

int main()
{
	// 지역변수 선언
	// 최초로 만들어진 시점을 선언이라 부른다
	int Value = 20;
	
	time_t DTime = time(0);
}
