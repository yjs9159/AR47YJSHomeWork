
#include <iostream>

// 진입점(여기서부터 실행될거야라는 지점)
// 진입점에서부터 실행흐름이 생긴다.
// 이 실행흐름을 함수라고 부르기로 함
int main() // <- int main()을 진입점으로 하기로 모두가 합의했다
{ // 함수의 시작
    std::cout << "Hello World!\n";
} // 함수의 끝

   